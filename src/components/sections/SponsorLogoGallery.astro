---
interface Logo {
  name: string;
  image: string;
  link: string;
}

interface Props {
  logos: Logo[];
}

const { logos } = Astro.props;
---

<section class="logo-gallery" aria-label="Sponsor logos">
  {
    logos.map((logo) => (
      <a href={logo.link} target="_blank" rel="noreferrer" aria-label={logo.name}>
        <img src={`${import.meta.env.BASE_URL}${logo.image.startsWith('/') ? logo.image.slice(1) : logo.image}`} alt={logo.name} loading="lazy" />
      </a>
    ))
  }
</section>

<style>
  .logo-gallery {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 220px));
    justify-content: center;
    justify-items: center;
    align-items: center;
    gap: 1rem 1.25rem;
    align-content: start;
  }

  .logo-gallery a {
    width: 100%;
    aspect-ratio: 16 / 10;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.7rem;
    text-decoration: none;
    border: 1px solid color-mix(in oklab, var(--border) 90%, white);
    border-radius: 1rem;
    background: var(--page-surface);
    box-shadow: 0 8px 24px rgba(15, 23, 42, 0.04);
    transition: opacity 180ms ease;
    max-width: 220px;
    justify-self: center;
  }

  .logo-gallery a:hover {
    opacity: 0.85;
  }

  .logo-gallery img {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: contain;
    object-position: center;
  }

  @media (max-width: 700px) {
    .logo-gallery {
      gap: 0.8rem;
    }

    .logo-gallery a {
      aspect-ratio: 16 / 10;
      padding: 0.6rem;
    }
  }
</style>
