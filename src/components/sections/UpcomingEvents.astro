---
interface Props {
  title?: string;
  calendarId?: string;
  height?: number;
}

const {
  title = 'Upcoming Events',
  calendarId = 'uavsatberkeley@gmail.com',
  height = 600,
} = Astro.props;

const calendarSrc = `https://calendar.google.com/calendar/embed?src=${encodeURIComponent(calendarId)}&ctz=${encodeURIComponent('America/Los_Angeles')}&mode=MONTH&showTitle=1&showNav=1&showDate=1&showPrint=0&showTabs=1&showCalendars=0`;
---

<section class="events-section">
  <h2>{title}</h2>
  <div class="calendar-embed-wrap">
    <iframe
      src={calendarSrc}
      class="calendar-embed"
      title="Upcoming Events"
      loading="lazy"
      referrerpolicy="no-referrer-when-downgrade"
      scrolling="no"
    ></iframe>
  </div>
</section>

<style>
  .events-section {
    display: grid;
    gap: 1rem;
  }

  .calendar-embed-wrap {
    width: 100%;
    border: 1px solid color-mix(in oklab, var(--border) 90%, white);
    border-radius: 1rem;
    overflow: hidden;
    background: var(--page-surface);
    box-shadow: 0 10px 30px rgba(15, 23, 42, 0.04);
  }

  .calendar-embed {
    width: 100%;
    min-height: 600px;
    border: 0;
    display: block;
  }
</style>
