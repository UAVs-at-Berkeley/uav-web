---
import NavbarClient from './NavbarClient';

interface Props {
  currentPath: string;
}

const { currentPath } = Astro.props;
const baseUrl = import.meta.env.BASE_URL;
---

<NavbarClient currentPath={currentPath} baseUrl={baseUrl} client:load />
